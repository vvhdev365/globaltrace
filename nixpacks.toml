[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.app_standalone:app --host 0.0.0.0 --port $PORT"
